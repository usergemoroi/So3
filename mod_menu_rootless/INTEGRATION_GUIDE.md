# Руководство по интеграции Mod Menu без ROOT

## 🎯 Обзор

Это полностью безрутовая система мод меню для Standoff 2, которая работает через:
- **Android Overlay** - меню отображается поверх игры без модификации рендера
- **DEX Injection** - динамическая загрузка кода в runtime
- **Native Hooks** - нативные перехваты без root прав
- **Advanced Protection** - многоуровневая защита от обнаружения

## 🚀 Быстрый старт

### Автоматический патчинг (Рекомендуется)

```bash
# 1. Соберите мод
cd mod_menu_rootless
chmod +x build.sh
./build.sh

# 2. Запатчите APK
cd tools
python3 apk_patcher.py path/to/standoff2.apk

# 3. Установите
adb install standoff2_modded_signed.apk
```

### Ручная интеграция

#### Шаг 1: Сборка компонентов

```bash
cd mod_menu_rootless

# Установите переменные окружения
export ANDROID_NDK_HOME=/path/to/ndk
export ANDROID_HOME=/path/to/sdk

# Соберите
./build.sh
```

Результат:
- `output/libmodmenu_noroot.so` - нативная библиотека
- `output/modmenu.dex` - DEX файл с Java классами

#### Шаг 2: Декомпиляция APK

```bash
# Установите apktool
apt-get install apktool  # или brew install apktool

# Декомпилируйте
apktool d standoff2.apk -o standoff2_decompiled
```

#### Шаг 3: Добавление файлов мода

```bash
cd standoff2_decompiled

# Скопируйте DEX в assets
cp ../mod_menu_rootless/output/modmenu.dex assets/

# Скопируйте библиотеку
cp ../mod_menu_rootless/output/libmodmenu_noroot.so lib/arm64-v8a/
```

#### Шаг 4: Патчинг AndroidManifest.xml

Откройте `AndroidManifest.xml` и добавьте:

```xml
<manifest>
    <!-- Добавьте разрешения -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <application>
        <!-- Добавьте сервис оверлея -->
        <service
            android:name="com.modmenu.loader.OverlayService"
            android:enabled="true"
            android:exported="false"/>
    </application>
</manifest>
```

#### Шаг 5: Внедрение загрузчика

Найдите главный класс Application в smali:

```bash
cd smali
# Ищите класс, наследующий Application
find . -name "*.smali" | xargs grep "Landroid/app/Application"
```

Откройте найденный файл и в методе `onCreate()` после `invoke-super` добавьте:

```smali
.method public onCreate()V
    .locals 0
    
    # Вызов родительского метода
    invoke-super {p0}, Landroid/app/Application;->onCreate()V
    
    # ← ДОБАВЬТЕ ЭТИ СТРОКИ ↓
    invoke-static {p0}, Lcom/modmenu/loader/DexInjector;->inject(Landroid/content/Context;)V
    
    invoke-static {p0}, Lcom/modmenu/loader/ModMenuLoader;->inject(Landroid/app/Application;)V
    # ← КОНЕЦ ДОБАВЛЕНИЯ
    
    return-void
.end method
```

#### Шаг 6: Пересборка и подпись

```bash
# Пересоберите APK
apktool b standoff2_decompiled -o standoff2_modded.apk

# Создайте ключ для подписи (если нет)
keytool -genkey -v -keystore debug.keystore -alias androiddebugkey \
    -keyalg RSA -keysize 2048 -validity 10000 -storepass android

# Подпишите APK
jarsigner -sigalg SHA1withRSA -digestalg SHA1 \
    -keystore debug.keystore -storepass android \
    standoff2_modded.apk androiddebugkey

# Или используйте apksigner (Android SDK)
apksigner sign --ks debug.keystore --ks-pass pass:android \
    --out standoff2_modded_signed.apk standoff2_modded.apk
```

#### Шаг 7: Установка

```bash
# Установите на устройство
adb install standoff2_modded_signed.apk

# Или скопируйте на телефон и установите вручную
```

## 🎮 Использование

### Первый запуск

1. **Разрешите отображение поверх других окон**
   - При первом запуске появится запрос на разрешение
   - Перейдите в настройки и разрешите
   - Вернитесь в игру

2. **Найдите кнопку меню**
   - В левом верхнем углу появится кнопка "Menu"
   - Кнопку можно перетаскивать

3. **Откройте меню**
   - Нажмите на кнопку "Menu"
   - Откроется полное меню мода

### Настройка ESP

```
ESP Settings:
├── Enable ESP ..................... Включить ESP
├── Box ESP ....................... Рамки вокруг игроков
├── Skeleton ESP .................. Скелет игроков
└── Health ESP .................... Отображение здоровья
```

### Настройка Aimbot

```
Aimbot Settings:
├── Enable Aimbot ................. Включить aimbot
├── Visible Only .................. Целиться только в видимых
├── FOV (10-180) .................. Угол обзора для захвата цели
└── Smooth (1.0-20.0) ............. Плавность наведения
```

## 🛡️ Защита и безопасность

### Встроенная защита

1. **Anti-Debug**
   - Детект через `/proc/self/status`
   - Проверка `ptrace`
   - Поиск процессов отладки

2. **Anti-Emulator**
   - Проверка файлов эмулятора
   - Анализ CPU характеристик
   - Проверка system properties

3. **Memory Integrity**
   - Checksum проверка кода
   - Обнаружение модификаций в памяти

4. **String Encryption**
   - XOR шифрование строк
   - Динамическая расшифровка в runtime

### Обход обнаружения

Мод использует несколько техник для обхода античитов:

```
✓ Overlay вместо модификации GL
✓ DEX injection вместо прямой модификации APK
✓ Нативные хуки без изменения игровых файлов
✓ Рандомизация адресов в памяти
✓ Скрытие от Android API проверок
```

## 🔧 Отладка

### Проверка логов

```bash
# Полные логи мода
adb logcat | grep -E "ModMenu|IL2CPP|Hooks|Protection|OverlayService"

# Только ошибки
adb logcat *:E | grep ModMenu

# Сохранить в файл
adb logcat > mod_debug.log
```

### Распространенные проблемы

**Проблема**: Меню не появляется
```
Решение:
1. Проверьте разрешение SYSTEM_ALERT_WINDOW в настройках
2. Посмотрите логи: adb logcat | grep OverlayService
3. Убедитесь, что DEX файл загружен: grep "DEX injected" в логах
```

**Проблема**: ESP/Aimbot не работает
```
Решение:
1. Проверьте, что libil2cpp.so загружен: adb logcat | grep IL2CPP
2. Убедитесь, что хуки установлены: grep "Hook placed" в логах
3. Проверьте версию игры - может потребоваться обновление оффсетов
```

**Проблема**: Игра крашится при запуске
```
Решение:
1. Проверьте, что библиотека собрана для правильной архитектуры
2. Отключите защиту от эмулятора (если используется эмулятор)
3. Проверьте правильность смали-патча
```

## 📱 Требования

### Минимальные требования

- Android 7.0+ (API 24+)
- ARMv8 (64-bit) процессор
- 100 MB свободного места
- Разрешение "Отображение поверх других окон"

### Рекомендуемые требования

- Android 10.0+ (API 29+)
- 4 GB RAM
- Без эмулятора (работает быстрее на реальном устройстве)

### Зависимости для сборки

```bash
# Ubuntu/Debian
apt-get install -y \
    openjdk-11-jdk \
    python3 \
    apktool \
    zipalign \
    aapt

# Скачайте Android NDK
wget https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
unzip android-ndk-r25c-linux.zip
export ANDROID_NDK_HOME=$PWD/android-ndk-r25c

# Скачайте Android SDK
wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
```

## 🔄 Обновление

### Обновление мода

```bash
# 1. Получите новую версию
git pull

# 2. Пересоберите
cd mod_menu_rootless
./build.sh

# 3. Перепатчите APK
cd tools
python3 apk_patcher.py path/to/standoff2.apk

# 4. Переустановите
adb install -r standoff2_modded_signed.apk
```

### Обновление оффсетов (при обновлении игры)

Если игра обновилась, нужно найти новые оффсеты:

1. Декомпилируйте новый APK
2. Найдите классы PlayerController, Camera и т.д. в libil2cpp.so
3. Обновите структуры в `il2cpp_noroot.hpp`
4. Пересоберите мод

## 📊 Архитектура

```
┌─────────────────────────────────────┐
│      Standoff 2 APK (Modded)        │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │   Java Layer                │   │
│  │  - ModMenuLoader            │   │
│  │  - OverlayService           │   │
│  │  - DexInjector              │   │
│  └─────────────────────────────┘   │
│             │                       │
│             ↓ JNI                   │
│  ┌─────────────────────────────┐   │
│  │   Native Layer (C++)        │   │
│  │  - IL2CPP Resolver          │   │
│  │  - Function Hooks           │   │
│  │  - ESP / Aimbot Logic       │   │
│  │  - Protection System        │   │
│  └─────────────────────────────┘   │
│             │                       │
│             ↓ Hooks                 │
│  ┌─────────────────────────────┐   │
│  │   Game Engine (Unity)       │   │
│  │  - libil2cpp.so             │   │
│  │  - libunity.so              │   │
│  │  - Game Logic               │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

## 🎓 Дополнительные ресурсы

- [Substrate Hooking](https://github.com/Cydia/substrate)
- [IL2CPP Reversing](https://katyscode.wordpress.com/2021/02/23/il2cpp-finding-obfuscated-global-metadata/)
- [Android Overlay Tutorial](https://developer.android.com/reference/android/view/WindowManager)
- [DEX File Format](https://source.android.com/devices/tech/dalvik/dex-format)

## ⚠️ Дисклеймер

Этот мод предназначен только для образовательных целей.
Использование модов в многопользовательских играх может:
- Нарушать условия использования
- Привести к бану аккаунта
- Испортить игровой опыт другим игрокам

Используйте на свой риск!

## 📝 Лицензия

Этот проект предоставляется "как есть" без каких-либо гарантий.

---

**Версия**: 2.0.0-rootless  
**Дата**: 2024  
**Статус**: Работает без ROOT ✅
