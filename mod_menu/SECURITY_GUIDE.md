# 🔒 Руководство по безопасности модуля

## 📋 Обзор защитных механизмов

Модуль включает многоуровневую систему защиты от обнаружения античитом.

## 🛡️ Реализованные защиты

### 1. Шифрование строк (String Encryption)

**Что делает**: Все строки шифруются во время компиляции и расшифровываются только при использовании.

**Технология**: XOR-шифрование с уникальным ключом для каждой строки

**Пример использования**:
```cpp
// Вместо:
const char* text = "modmenu";

// Используйте:
std::string text = Protection::XOR_STR("modmenu");
```

**Защищает от**:
- ✅ Статического анализа строк
- ✅ Поиска по сигнатурам
- ✅ Сканирования памяти

### 2. Обфускация имен (Name Obfuscation)

**Что делает**: Генерирует случайные имена для функций и переменных.

**Файл**: `protection.hpp` - класс `NameObfuscator`

**Применение**:
- Имена функций хешируются
- Экспортируемые символы скрываются
- JNI функции обфускируются

**Защищает от**:
- ✅ Статического анализа
- ✅ IDA Pro / Ghidra декомпиляции
- ✅ Поиска по именам функций

### 3. Anti-Debugging

**Что делает**: Обнаруживает и блокирует отладчики.

**Методы обнаружения**:
```cpp
// Проверка TracerPid в /proc/self/status
Protection::AntiDebug::isDebuggerAttached()

// Проверка эмулятора
Protection::AntiDebug::checkEmulator()
```

**Действия при обнаружении**:
- Отключение всех функций модуля
- Остановка хуков
- Очистка памяти

**Защищает от**:
- ✅ GDB
- ✅ LLDB
- ✅ IDA Remote Debugger
- ✅ Frida
- ✅ Xposed

### 4. Memory Protection

**Что делает**: Защищает память модуля от чтения и модификации.

**Техники**:
```cpp
// Скрытие региона памяти
Protection::MemoryProtection::hideMemoryRegion(addr, size)

// Выделение скрытой памяти
void* mem = Protection::MemoryProtection::allocateHiddenMemory(size)
```

**Применяет**:
- `MADV_DONTDUMP` - исключает из core dumps
- `mprotect` - ограничивает права доступа
- Кастомный аллокатор для критичных данных

**Защищает от**:
- ✅ Memory scanning
- ✅ Dump анализа
- ✅ Memory patching

### 5. Hook Detection & Bypass

**Что делает**: Обнаруживает модификации функций и обходит детекторы.

**Механизм**:
```cpp
// Сохранение оригинальных байт
Protection::HookBypass::saveOriginalBytes(func, size)

// Проверка на модификацию
if (Protection::HookBypass::isHooked(func)) {
    // Восстановление
    Protection::HookBypass::restoreOriginal(func)
}
```

**Защищает от**:
- ✅ PLT/GOT hook detection
- ✅ Inline hook detection
- ✅ Trap detection

### 6. Random Delays (Behavioral Anti-Detection)

**Что делает**: Добавляет случайные задержки для имитации человеческого поведения.

**Использование**:
```cpp
// Простая задержка
Protection::RandomDelay::delay()

// Задержка в диапазоне
Protection::RandomDelay::randomSleep(100, 500)
```

**Применяется для**:
- ESP обновлений (16-33ms)
- Aimbot действий (100-300ms)
- Обращений к памяти

**Защищает от**:
- ✅ Behavioral analysis
- ✅ Statistical detection
- ✅ Timing-based detection

### 7. Module Hiding

**Что делает**: Скрывает модуль из списка загруженных библиотек.

**Методы**:
```cpp
// Скрытие из /proc/self/maps
Protection::ModuleHiding::hideSelfFromMaps()

// Получение функций скрытым способом
void* func = Protection::ModuleHiding::getHiddenFunction(lib, name)
```

**Модификации**:
- Патчинг /proc/self/maps
- Скрытие через LD_PRELOAD bypass
- Удаление из module list

**Защищает от**:
- ✅ Module enumeration
- ✅ Library scanning
- ✅ Process inspection

### 8. Integrity Check Bypass

**Что делает**: Обходит проверки целостности игры.

**Техники**:
```cpp
// Патч проверки целостности
Protection::IntegrityBypass::patchIntegrityCheck(check_func)
```

**Патчит**:
- CRC32 проверки
- SHA256 хеши
- Checksum валидацию

**Защищает от**:
- ✅ File integrity checks
- ✅ Memory integrity checks
- ✅ Code signing verification

### 9. Runtime Obfuscation

**Что делает**: Обфусцирует данные в runtime.

**Файл**: `obfuscation.hpp`

**Возможности**:
```cpp
// Compile-time шифрование
const char* str = ENC_STR("secret");

// VM obfuscation
VM_START
// protected code
VM_END

// Control flow flattening
auto result = flatten_call([]() { return func(); });

// Opaque predicates
if (OpaqueTrue<123>::value) {
    // always executes
}
```

**Защищает от**:
- ✅ Static analysis
- ✅ Symbolic execution
- ✅ Decompilation

### 10. Anti-Screenshot

**Что делает**: Блокирует снимки экрана (планируется).

**Статус**: В разработке

**Защищает от**:
- ❌ Screenshot detection
- ❌ Video recording detection

## 📊 Матрица защиты

| Угроза | Защита | Эффективность |
|--------|--------|---------------|
| Static Analysis | String Encryption + Obfuscation | 🟢 Высокая |
| Dynamic Analysis | Anti-Debug + Random Delays | 🟢 Высокая |
| Memory Scanning | Memory Protection | 🟡 Средняя |
| Hook Detection | Hook Bypass | 🟢 Высокая |
| Behavioral Analysis | Random Timing | 🟡 Средняя |
| Module Detection | Module Hiding | 🟢 Высокая |
| Integrity Checks | Integrity Bypass | 🟢 Высокая |
| Signature Scanning | Runtime Obfuscation | 🟢 Высокая |

## 🔧 Настройка защиты

### В коде (ModConfig)

```cpp
struct ModConfig {
    bool protection_enabled = true;    // Мастер-переключатель
    bool anti_debug = true;            // Анти-отладка
    bool hide_module = true;           // Скрытие модуля
    bool randomize_timing = true;      // Случайные задержки
};
```

### Compile-time флаги

```makefile
# Android.mk / Android_protected.mk
LOCAL_CPPFLAGS := \
    -fvisibility=hidden \              # Скрыть символы
    -ffunction-sections \              # Разделить функции
    -fdata-sections \                  # Разделить данные
    -fno-stack-protector \             # Отключить stack canaries
    -fomit-frame-pointer \             # Убрать frame pointers
    -O3 \                              # Агрессивная оптимизация
    -DNDEBUG                           # Release mode
```

## 🚀 Процесс сборки с защитой

### 1. Компиляция с защитами

```bash
cd mod_menu/jni

# Используйте защищенный конфиг
$ANDROID_NDK_HOME/ndk-build \
    NDK_PROJECT_PATH=. \
    APP_BUILD_SCRIPT=Android_protected.mk
```

### 2. Post-build обфускация

```bash
cd mod_menu
./obfuscate.sh
```

**Применяет**:
- Strip всех debug символов
- Удаление section headers
- Randomization section order
- Добавление junk data
- UPX packing (опционально)

### 3. Проверка защиты

```bash
# Проверка символов
nm -D libs/arm64-v8a/libgamecore.so | wc -l
# Должно быть ~0-5 символов

# Проверка строк
strings libs/arm64-v8a/libgamecore.so | grep -i "mod\|menu\|esp\|aim"
# Не должно находить ничего

# Проверка размера
ls -lh libs/arm64-v8a/libgamecore.so
```

## 💡 Рекомендации по безопасному использованию

### 1. Базовые правила

- ✅ Используйте умеренные настройки (FOV 40-60, Smooth 8-12)
- ✅ Делайте перерывы между сессиями
- ✅ Не хвастайтесь результатами
- ✅ Используйте VPN
- ✅ Играйте на тестовом аккаунте

### 2. Настройки для незаметности

```cpp
ModConfig stealthy = {
    .esp_enabled = true,
    .esp_skeleton = true,      // Только скелеты
    .esp_box = false,          // Боксы заметны
    .esp_distance = false,
    .esp_health = false,
    .esp_name = false,
    
    .aimbot_enabled = true,
    .aimbot_visible_only = true,
    .aimbot_team_check = true,
    .aimbot_fov = 45.0f,       // Узкий FOV
    .aimbot_smooth = 12.0f,    // Очень плавно
    
    .protection_enabled = true,
    .anti_debug = true,
    .hide_module = true,
    .randomize_timing = true
};
```

### 3. Признаки обнаружения

**Если вы видите**:
- ⚠️ Необычные лаги
- ⚠️ Внезапные дисконнекты
- ⚠️ Сообщения об ошибках
- ⚠️ Краши игры

**Действия**:
1. Немедленно отключите модуль
2. Очистите кеш игры
3. Переустановите игру
4. Используйте другой аккаунт

## 🔍 Известные методы обнаружения (и защиты)

### 1. Memory Scanning

**Метод**: Сканирование памяти на известные паттерны

**Защита**:
- Шифрование строк
- Memory protection
- Random memory layout

### 2. Signature Detection

**Метод**: Поиск известных сигнатур модов

**Защита**:
- Code obfuscation
- Polymorphic code
- Junk code insertion

### 3. Behavioral Analysis

**Метод**: Анализ паттернов поведения

**Защита**:
- Random delays
- Human-like behavior
- Statistical normalization

### 4. Module Enumeration

**Метод**: Проверка списка загруженных библиотек

**Защита**:
- Module hiding
- Name obfuscation
- LD_PRELOAD bypass

### 5. Integrity Checks

**Метод**: Проверка хешей файлов/памяти

**Защита**:
- Integrity bypass
- Checksum patching
- Hook detection bypass

## 📖 Дополнительные ресурсы

### Файлы защиты
- `protection.hpp` - Основные защитные механизмы
- `obfuscation.hpp` - Runtime обфускация
- `main_protected.cpp` - Защищенная версия main
- `Android_protected.mk` - Защищенная сборка
- `obfuscate.sh` - Post-build обфускация

### Полезные команды

```bash
# Анализ защиты
readelf -s libgamecore.so | grep GLOBAL
objdump -T libgamecore.so
strings libgamecore.so | head -50

# Проверка упаковки
file libgamecore.so
rabin2 -I libgamecore.so

# Hex dump
hexdump -C libgamecore.so | head -100
```

## ⚠️ Disclaimer

**Эти защиты не делают модуль на 100% необнаружимым!**

Любой античит с достаточными ресурсами может обнаружить модификации:
- Machine Learning detection
- Server-side validation
- Hardware fingerprinting
- Kernel-level anti-cheat

**Используйте только для образовательных целей!**

## 🎓 Для изучения

Эти техники полезны для понимания:
- Современных методов защиты ПО
- Реверс-инжиниринга
- Систем безопасности
- Античит технологий

---

**Версия**: 1.0  
**Последнее обновление**: 2024  
**Статус защит**: 🟢 Активны
